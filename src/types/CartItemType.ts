type CartItemType = {
  quantity: number;
  productId: string;
  productName: string;
  description: string;
  imageUrl: string;
  price: number;
  totalPrice: number;
};

export default CartItemType;
